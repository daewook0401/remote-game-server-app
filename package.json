{
  "name": "remote-game-server-app",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/desktop"
  ],
  "scripts": {
    "desktop:dev": "npm run dev -w apps/desktop",
    "desktop:build": "npm run build -w apps/desktop",
    "desktop:typecheck": "npm run typecheck -w apps/desktop"
  }
}

