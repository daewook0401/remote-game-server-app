# 2026-04-28 오늘 할 일

## 작업 목록

| 작업 | 상태 | 설명 |
| --- | --- | --- |
| task_1 | 승인대기 | 23단계 원격 서버 선택/삭제, Minecraft 생성 전 검증, Linux Agent Release 배포 완료 후 실제 원격 테스트 피드백 대기 |

## 오늘 결정한 사항

- `AGENT.md`를 이번 프로젝트의 작업 규칙으로 적용한다.
- 원격 게임 서버 오픈 / 점프 접속 기능은 먼저 수행계획서를 작성하고 승인 후 다음 단계로 진행한다.
- 사용자 접점은 웹 UI가 아니라 설치형 프로그램 UI로 본다.
- 현재 작업 공간에서는 수동 TCP 검증을 수행하지 않고, 추후 Git 레포가 추가될 때 검증 절차를 넣는다.
- UDP와 Nginx Stream은 우선 자동 원격 수정이 아니라 사용자 안내 가이드 제공 대상으로 본다.
- 수행계획서 승인을 받아 구현계획서를 작성했다.
- 구현계획서에 작업 언어와 환경을 별도 섹션으로 보강했다.
- GitHub 원격 저장소 `https://github.com/daewook0401/remote-game-server-app.git`를 `origin`으로 연결했다.
- 초기 개발 언어 기준은 TypeScript, Electron + React, Go, SQL, Docker/YAML, Markdown으로 정했다.
- 구현계획서 승인을 받아 1단계 단계별 보고서를 작성했다.
- 2단계 승인을 받아 Electron + React + TypeScript, Go Agent, Go Relay 기본 구조를 생성했다.
- 현재 환경에는 Go가 없어 Go 빌드/테스트/gofmt는 실행하지 못했다.
- Go 설치 경로를 직접 찾아 Agent/Relay `go test`와 `go build`를 실행했다.
- Desktop UI를 layout, page, component, data, type 단위로 분리했다.
- Go 빌드 산출물 `.exe`가 추적되지 않도록 `.gitignore`에 추가했다.
- Desktop UI 공개 시작/중지 상태 모델을 추가했다.
- Relay 포트 할당/회수 API 초안을 코드와 문서에 반영했다.
- Agent FRP 설정 요청 모델을 코드와 테스트에 반영했다.
- 제품 범위가 서버 공개뿐 아니라 로컬/원격/클라우드 서버 연결, Minecraft 서버 생성, Docker 관리, 콘솔 접근까지 포함된다는 피드백을 반영했다.
- Desktop UI를 서버 관리, 콘솔, 외부 공개, 안내 가이드 화면 구조로 바꿨다.
- Agent Docker 관리 계약 초안을 추가했다.
- 서버 관리 중심 UI와 Agent Docker 계약 초안을 코드로 반영하고 검증했다.
- Desktop Agent/Relay client 계층 초안을 추가했다.
- Agent HTTP API 서버 초안을 추가하고 `/healthz`, `/docker/minecraft` 응답을 검증했다.
- Agent Docker adapter 경계를 추가했다.
- 기본 Docker 제어는 memory mode, 선택적 실제 제어는 `AGENT_DOCKER_MODE=cli`로 분리했다.
- Docker CLI `29.4.0`을 확인했다.
- Desktop `Agent 설치 확인` 버튼을 Agent `/docker/status` API에 연결했다.
- 서버 관리 화면에 Agent/Docker 상태 패널을 추가했다.
- 개발 UI 흰 화면 원인을 확인하고 React root mount 진입점 `main.tsx`를 추가했다.
- memory/cli mode 상태를 UI에서 더 명확히 구분하도록 보강했다.
- Docker CLI mode 실행 가이드를 추가했다.
- 콘솔 화면을 Agent `/docker/containers/console` snapshot API에 연결했다.
- 컨테이너 시작/중지/삭제 버튼을 Agent `/docker/containers/action` API에 연결했다.
- Docker CLI mode에서 생성/시작/중지/삭제 작업에 2단계 확인 UX를 추가했다.
- 작업 진행 보고는 다음 단계부터 더 큰 기능 묶음 단위로 진행한다.
- 외부 공개 화면을 Relay 포트 할당/회수 API와 연결했다.
- Agent/Relay API에 개발 UI용 CORS 처리를 추가했다.
- 서버 생성 설정 패널을 추가해 실행 위치, 게임, 포트, 메모리, EULA를 선택하도록 했다.
- SSH/Agent/Docker 확인 흐름과 Docker label 기반 추적 기준을 문서화했다.
- 15단계 완료 후 첫 기준점 커밋 `26b96a3 feat: bootstrap remote game server app`를 `origin/main`에 푸시했다.
- 원격/클라우드 서버 등록 UI와 선택 서버 Agent URL 기준 API 호출 흐름을 추가했다.
- 등록된 서버 목록을 로컬 저장소에 저장하고, Agent token 기반 선택형 인증 모델을 추가했다.
- SSH 서버 등록 시 OS 선택, 패스워드/키 인증 선택, 원격 OS 비교를 추가했다.
- SSH 테스트에서 Docker 설치/준비 상태, Agent 포트, Agent API 접근 상태를 확인하도록 확장했다.
- SSH로 Linux Agent가 없으면 다운로드/설치/실행하고, 있으면 연결 확인하는 Agent 준비 흐름을 추가했다.
- Electron main의 SSH/Agent 로직을 모듈로 분리하고, Agent 준비 결과를 서버 카드에 반영했다.
- GitHub Release 자동 업로드는 이번 단계에서 제외했다.
- Docker 미설치, daemon 미실행, 권한 부족 상태를 구분하고 OS별 조치 안내를 표시하도록 했다.
- 실제 원격 서버 테스트를 위해 서버 카드 선택/삭제와 Minecraft 생성 전 체크리스트를 추가했다.
- SSH 자동 설치 흐름을 위해 Linux Agent 바이너리를 GitHub Release asset으로 배포하기로 했다.

## 오늘 받은 피드백

- 원격 게임 서버 오픈 / 점프 접속 문서는 계획서 성격이다.
- 이번 프로젝트는 `AGENT.md` 기준 절차로 진행한다.
- 웹 UI가 아닌 프로그램 파일로 설치해서 실행하는 방식이며, 디자인도 실제 프로그램처럼 구성한다.
- 현재 작업 공간은 수동 TCP 검증이 불가능하므로 추후 Git 레포가 추가될 때 검증 절차를 넣는다.
- UDP, Nginx Stream은 실제 접속 후 원격 수정이 어려우므로 사용자 안내 가이드로 제공한다.
- 구현계획서에 작업 언어와 환경이 누락되어 있었다.
- 실제 시작 프로젝트의 Git 레포를 연결하고 개발 언어를 명시해야 한다.
- Go 설치 후 검증을 다시 실행해야 한다.
- `App.tsx`에 UI를 몰아넣지 말고 레이아웃, 컴포넌트, 페이지 단위로 분리해야 한다.
- 로컬 또는 클라우드 서버에 Minecraft 서버를 생성/관리할 수 있어야 하며, Docker 콘솔 접속 같은 운영 기능이 필요하다.
- 개발 UI가 흰 화면만 보인다는 피드백을 받았다.
- 서버 생성 시 게임, 포트, 실행 위치, SSH 연결 여부를 선택해야 하며 Docker 생성 확인 방식도 필요하다는 피드백을 받았다.
